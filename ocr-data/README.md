# Offline OCR data

`eng.traineddata.gz` is the English Tesseract language model downloaded from
`https://tessdata.projectnaptha.com/4.0.0/eng.traineddata.gz`.

It is kept in the project so deterministic replay can extract canvas pixels
without an OpenAI key or an online OCR service.
